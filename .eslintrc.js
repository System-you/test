module.exports = {
    extends: [
      'react-app',
    ],
    rules: {
      'no-unused-vars': 'warn',  // Change this to 'off' if you want to ignore unused variables.
      'jsx-a11y/anchor-is-valid': 'off',  // Disable the anchor-is-valid rule if it's too strict for your needs.
    },
  };